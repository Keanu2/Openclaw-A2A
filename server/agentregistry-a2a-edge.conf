[Service]
# Keep the registry API on public 8000, but move its embedded WebSocket tunnel
# behind the shared Nginx TCP/8001 edge.
Environment=A2X_TUNNEL_HOST=127.0.0.1
Environment=A2X_TUNNEL_PORT=18001
